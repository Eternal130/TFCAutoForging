
name: Release tagged build (disabled)

# 已由 release-multi.yml 取代（多分支集中发布）。
# 保留此文件仅供回退：把 tags 改回 '*' 即可恢复原行为。

on:
  push:
    tags: [ 'disabled-*' ]

permissions:
  contents: write

jobs:
  release-tags:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/release-tags.yml@master
    secrets: inherit
